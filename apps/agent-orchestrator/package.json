{
  "name": "orchestrator-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.supported.json",
    "lint": "eslint worker.ts src/worker.contract.test.ts",
    "start:worker": "tsx worker.ts",
    "dev": "tsx worker.ts",
    "test": "node --import tsx --test \"src/worker.contract.test.ts\"",
    "typecheck": "tsc -p tsconfig.supported.json --noEmit"
  },
  "dependencies": {
    "@birthub/llm-client": "workspace:*",
    "@birthub/queue": "workspace:*",
    "@birthub/shared-types": "workspace:*",
    "axios": "^1.13.6",
    "bullmq": "^5.71.0",
    "dotenv": "^17.3.1",
    "ioredis": "^5.10.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
