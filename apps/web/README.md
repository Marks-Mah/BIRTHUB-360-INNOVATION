# @birthub/web

Entrypoint canônico do ciclo para o app web no Turborepo.

- `pnpm --filter @birthub/web dev` inicia o dashboard (`@birthub/dashboard`).
- `pnpm --filter @birthub/web build` executa o build do dashboard.
