# @birthub/worker

Entrypoint canônico do ciclo para workers no Turborepo.

- `pnpm --filter @birthub/worker dev` inicia o worker principal (`orchestrator-worker`).
- `pnpm --filter @birthub/worker test` executa os testes do worker.
