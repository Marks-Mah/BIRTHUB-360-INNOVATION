# Execução dos próximos 20 passos lógicos — Ciclos CODEX 01-10

Fonte: prompts históricos em `CHECKLIST E PROMPTS/*_CODEX.html` recuperados do commit `9ab569e` e validação do estado atual do repositório.

## Metodologia curta
1. Recuperar prompt histórico do ciclo no Git.
2. Extrair backlog tabular (ID e descrição).
3. Selecionar os próximos 20 itens do ciclo (ordem do prompt).
4. Executar validação lógica no repositório atual (busca por evidências).
5. Registrar resultado e próxima ação objetiva.

## Ciclo 01 — 20 próximos passos executados
| Passo | Execução lógica realizada | Resultado |
|---|---|---|
| `1.1.C1` — Criar Turborepo com apps/web, apps/api e apps/worker configurados | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |
| `1.1.C2` — Configurar tsconfig base com strict mode e paths de importação entre packages | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/scripts/schedule-recurring-jobs.ts`); status: **parcial/implementado**. |
| `1.1.C3` — Configurar eslint com plugins typescript e import-order | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/scripts/schedule-recurring-jobs.ts`); status: **parcial/implementado**. |
| `1.1.C4` — Configurar prettier com regras compartilhadas em packages/config/prettier | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/scripts/schedule-recurring-jobs.ts`); status: **parcial/implementado**. |
| `1.1.C5` — Criar script raiz dev que sobe todos os apps em paralelo com Turbo | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/package.json`); status: **parcial/implementado**. |
| `1.10.C1` — Criar CHECKLIST_MASTER.md com status atual de todos os 100 itens do Ciclo 1 | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `1.10.C2` — Criar CHECKLIST_LOG.md com registro de primeira execução, datas e evidências | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |
| `1.10.C3` — Garantir que todos os 10 ADRs de Jules estão referenciados no código-fonte | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./CHECKLIST_MASTER.md`); status: **parcial/implementado**. |
| `1.10.C4` — Executar smoke tests de todos os módulos e apresentar evidência de 100% green | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `1.10.C5` — Confirmar que setup-local.sh funciona em máquina limpa sem nenhum pré-requisito manual | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `1.2.C1` — Criar workflow GitHub Actions de CI: lint + typecheck + test + build em paralelo | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/migrations/20260223101000_billing_usage/migration.sql`); status: **parcial/implementado**. |
| `1.2.C2` — Configurar cache de dependências do Turbo no CI para builds rápidos | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/scripts/schedule-recurring-jobs.ts`); status: **parcial/implementado**. |
| `1.2.C3` — Adicionar gitleaks ao CI como gate bloqueante de push com secret detectado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |
| `1.2.C4` — Configurar branch protection rules: require PR, require CI green, no force push | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/scripts/schedule-recurring-jobs.ts`); status: **parcial/implementado**. |
| `1.2.C5` — Criar workflow de CI separado para security scan (semgrep + dependency check) | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/migrations/20260223101000_billing_usage/migration.sql`); status: **parcial/implementado**. |
| `1.3.C1` — Implementar logger estruturado (pino/winston) com campos requestId tenantId userId level | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/utils/src/logger.ts`); status: **parcial/implementado**. |
| `1.3.C2` — Integrar Sentry em apps/web com captura de erros de client-side e sessão | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/product/sampling-analysis.md`); status: **parcial/implementado**. |
| `1.3.C3` — Integrar Sentry em apps/api com captura de exceções não tratadas e contexto de request | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/product/sampling-analysis.md`); status: **parcial/implementado**. |
| `1.3.C4` — Instalar OpenTelemetry SDK no API com instrumentação automática de HTTP e Prisma | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/utils/src/tracing.ts`); status: **parcial/implementado**. |
| `1.3.C5` — Criar middleware de correlação que propaga requestId entre web, api e worker | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |

## Ciclo 02 — 20 próximos passos executados
| Passo | Execução lógica realizada | Resultado |
|---|---|---|
| `2.1.C1` — Adicionar tenantId obrigatório como primeiro campo de index em todas as tabelas de negócio | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/src/index.ts`); status: **parcial/implementado**. |
| `2.1.C2` — Criar packages/database/src/repositories/base.repo.ts com tenantId injetado em toda query | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/scripts/archive-agent-logs.sql`); status: **parcial/implementado**. |
| `2.1.C3` — Implementar middleware de contexto de tenant que injeta tenant de forma imutável no request | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `2.1.C4` — Criar testes unitários: query sem tenantId deve lançar exceção TenantRequiredError | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `2.1.C5` — Atualizar seed com 2 tenants completamente independentes e dados não-compartilhados | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/src/index.ts`); status: **parcial/implementado**. |
| `2.10.C1` — Executar isolation suite completa e apresentar relatório com 0 falhas como evidência | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/migrations/20260223100000_tenant_foundation/migration.sql`); status: **parcial/implementado**. |
| `2.10.C2` — Corrigir todos os findings técnicos do relatório de Tenant Security Review de Jules | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `2.10.C3` — Confirmar que todos os ADRs Jules do Ciclo 2 têm implementação correspondente no código | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `2.10.C4` — Atualizar CHECKLIST_MASTER.md com status real de todos os 100 itens do Ciclo 2 | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `2.10.C5` — Executar explain-analyze das 10 queries críticas e confirmar uso de indexes | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `2.2.C1` — Padronizar indexes compostos (tenantId, id) em todas as tabelas com queries frequentes | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/migrations/20260223090000_sprint3_operational_data/migration.sql`); status: **parcial/implementado**. |
| `2.2.C2` — Criar EXPLAIN ANALYZE das 10 queries mais críticas e documentar plano de execução | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `2.2.C3` — Implementar query timeout de 5s como default no Prisma client com erro tratado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `2.2.C4` — Criar testes de performance: query com 10k registros de tenant único < 100ms | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `2.2.C5` — Implementar paginação cursor-based em todos os endpoints de listagem | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `2.3.C1` — Implementar RLS policies no Postgres para tabelas: organizations members agents workflows | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-migration-checklist.md`); status: **parcial/implementado**. |
| `2.3.C2` — Criar função SQL get_current_tenant_id() que lê claim do JWT em contexto de sessão | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `2.3.C3` — Criar testes SQL de policy: SELECT de tenant A com credencial de tenant B retorna vazio | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `2.3.C4` — Documentar como habilitar, desabilitar e testar RLS por tabela em ambiente de dev | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/security/analise_risco_conectores.md`); status: **parcial/implementado**. |
| `2.3.C5` — Criar migration para habilitar RLS em todas as tabelas críticas com FORCE ROW LEVEL SECURITY | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |

## Ciclo 03 — 20 próximos passos executados
| Passo | Execução lógica realizada | Resultado |
|---|---|---|
| `3.1.C1` — Implementar Auth.js ou Supabase Auth conforme decisão do ADR-010 de Jules | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./infra/terraform/modules/secrets.tf`); status: **parcial/implementado**. |
| `3.1.C2` — Remover todos os mocks e stubs de autenticação do codebase e histórico git | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `3.1.C3` — Criar testes de autenticação: login, logout, sessão expirada, token inválido, 2FA | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `3.1.C4` — Implementar logout total: revogar todos os tokens e sessões do usuário em 1 ação | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/security-pr-acceptance.md`); status: **parcial/implementado**. |
| `3.1.C5` — Criar endpoint de introspection para validar token de API key e retornar contexto | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `3.10.C1` — Corrigir todos os findings de segurança de severidade alta ou crítica do relatório Jules | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `3.10.C2` — Executar RBAC suite completa e apresentar 100% de cobertura como evidência | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `3.10.C3` — Confirmar que todos os ADRs Jules do Ciclo 3 têm implementação documentada | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `3.10.C4` — Garantir que não há endpoint sem guard de autenticação exceto os explicitamente públicos | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `3.10.C5` — Executar OWASP ZAP baseline scan contra staging e apresentar resultado limpo | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `3.2.C1` — Implementar cookie de sessão com flags httpOnly Secure SameSite=Strict | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `3.2.C2` — Implementar rotate automático de refresh token a cada uso com invalidação do anterior | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/auth/src/__tests__/auth.test.ts`); status: **parcial/implementado**. |
| `3.2.C3` — Criar endpoint de logout global que revoga todas as sessões do usuário | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/integrations/src/clients/fiscal.ts`); status: **parcial/implementado**. |
| `3.2.C4` — Criar tela de sessões ativas com device, IP, última atividade e botão de revogar | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `3.2.C5` — Implementar detecção de sessão concorrente com alerta de novo dispositivo | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `3.3.C1` — Definir e implementar roles: owner, admin, member, readonly com permissões no banco | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `3.3.C2` — Criar decorator @RequireRole e guard de autorização no API para cada endpoint | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `3.3.C3` — Implementar permission check no nível de query: JOIN com memberships por tenantId | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/auth/src/__tests__/auth.test.ts`); status: **parcial/implementado**. |
| `3.3.C4` — Criar testes de RBAC cobrindo todos os roles em todos os endpoints críticos | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `3.3.C5` — Criar UI de gestão de roles: owner pode promover a admin, admin pode promover a member | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |

## Ciclo 04 — 20 próximos passos executados
| Passo | Execução lógica realizada | Resultado |
|---|---|---|
| `4.1.C1` — Criar packages/agents-core/src/types/ com Agent Skill Tool Policy Execution em TypeScript | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/scripts/archive-agent-logs.sql`); status: **parcial/implementado**. |
| `4.1.C2` — Implementar AgentManifest como JSON Schema com validação Zod estrita | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `4.1.C3` — Criar parser de manifest com erros descritivos apontando campo e motivo exato | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/load-test/bullmq-load-test.ts`); status: **parcial/implementado**. |
| `4.1.C4` — Criar testes unitários do parser: manifest válido, inválido, parcial, versão incompatível | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `4.1.C5` — Publicar packages/agents-core como package interno com typedoc gerado automaticamente | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/scripts/archive-agent-logs.sql`); status: **parcial/implementado**. |
| `4.10.C1` — Executar suíte completa de testes do Agent Core e apresentar cobertura > 80% | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `4.10.C2` — Corrigir todos os achados técnicos do relatório Jules do gate do Ciclo 4 | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `4.10.C3` — Confirmar que todos os ADRs Jules do Ciclo 4 têm implementação documentada | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `4.10.C4` — Executar teste de carga de 50 execuções paralelas e apresentar métricas como evidência | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/load-test/bullmq-load-test.ts`); status: **parcial/implementado**. |
| `4.10.C5` — Garantir que PolicyEngine bloqueia 100% das ações proibidas em todos os testes | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-migration-checklist.md`); status: **parcial/implementado**. |
| `4.2.C1` — Criar packages/agents-registry com CRUD de AgentVersion: create, publish, deprecate, rollback | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/scripts/archive-agent-logs.sql`); status: **parcial/implementado**. |
| `4.2.C2` — Implementar semver strict: major (breaking), minor (feature), patch (fix) com changelog | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `4.2.C3` — Criar hashing SHA256 de manifest e armazenar digest para verificação de integridade | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `4.2.C4` — Criar índice de agentes por tenant com busca por tag, versão e status | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `4.2.C5` — Implementar rollback de versão: reverter agente para versão anterior com audit log | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-migration-checklist.md`); status: **parcial/implementado**. |
| `4.3.C1` — Criar filas BullMQ com prioridades: agent-high, agent-normal, agent-low por tenant | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `4.3.C2` — Criar PlanExecutor: recebe AgentExecution, gera plano de tool calls, executa em sequência | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |
| `4.3.C3` — Implementar idempotência por executionId: reprocessar mesmo job não duplica efeitos colaterais | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/api/operacao-api-gateway.md`); status: **parcial/implementado**. |
| `4.3.C4` — Criar retry com exponential backoff: 1s, 5s, 30s com jitter e máximo de 5 tentativas | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/src/workers/base-worker.ts`); status: **parcial/implementado**. |
| `4.3.C5` — Implementar graceful shutdown do worker com draining de jobs em execução | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/configuration-rotation.md`); status: **parcial/implementado**. |

## Ciclo 05 — 20 próximos passos executados
| Passo | Execução lógica realizada | Resultado |
|---|---|---|
| `5.1.C1` — Criar packages/agent-packs/corporate-v1/ com estrutura: manifest.json prompts/ config/ tests/ | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/scripts/archive-agent-logs.sql`); status: **parcial/implementado**. |
| `5.1.C2` — Implementar script de validação de todos os manifests do catálogo no CI | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/package.json`); status: **parcial/implementado**. |
| `5.1.C3` — Criar manifest JSON para CEO Agent Pack com skills e tools documentados | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `5.1.C4` — Criar manifest JSON para CRO, CMO, CFO, CTO, COO Agent Packs | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `5.1.C5` — Criar manifest JSON para Legal, RH, CS, Sales, Finance, Ops Agent Packs | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `5.10.C1` — Executar smoke tests de todos os packs e apresentar 100% de sucesso como evidência | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `5.10.C2` — Corrigir manifests inválidos ou incompletos apontados no relatório Jules | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/agents-core/src/manifest/parser.ts`); status: **parcial/implementado**. |
| `5.10.C3` — Confirmar que docs geradas cobrem 100% dos agentes do catálogo sem campos vazios | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `5.10.C4` — Garantir que dry-run de cada agente completa sem erro de runtime ou schema | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-migration-checklist.md`); status: **parcial/implementado**. |
| `5.10.C5` — Executar testes de regressão de todos os packs e apresentar resultado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `5.2.C1` — Implementar sistema de tags em manifests: domain level persona use-case industry | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./CHECKLIST_MASTER.md`); status: **parcial/implementado**. |
| `5.2.C2` — Criar índice de busca full-text dos manifests com ranking por relevância | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `5.2.C3` — Criar página de Agent Marketplace com filtros por tag, domain e level | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `5.2.C4` — Implementar sugestão de agentes baseada no perfil de uso do tenant | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `5.2.C5` — Criar API de busca de agentes com paginação e facets de filtro | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `5.3.C1` — Criar skill template analyzer com inputSchema análise e outputSchema com score e insights | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |
| `5.3.C2` — Criar skill template generator com inputSchema brief e outputSchema com artefato gerado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `5.3.C3` — Criar skill template monitor com inputSchema KPI e outputSchema com alertas | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `5.3.C4` — Criar skill template orchestrator que chama outros agentes em sequência | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `5.3.C5` — Criar skill template reporter com inputSchema métricas e outputSchema PDF/Markdown | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |

## Ciclo 06 — 20 próximos passos executados
| Passo | Execução lógica realizada | Resultado |
|---|---|---|
| `6.1.C1` — Criar schema Prisma: Workflow WorkflowVersion WorkflowRun WorkflowStep RunLog | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `6.1.C2` — Criar validador de workflow: detectar ciclos, steps órfãos e condições impossíveis | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `6.1.C3` — Criar UI de builder de workflow com nós de trigger, condição, ação e agente | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `6.1.C4` — Implementar export e import de workflow como JSON versionado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/src/workers/base-worker.ts`); status: **parcial/implementado**. |
| `6.1.C5` — Criar testes unitários do validador com grafos válidos e inválidos | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `6.10.C1` — Executar e2e suite completa e apresentar cobertura de tipos de step e de falhas | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `6.10.C2` — Corrigir todos os achados técnicos do relatório Jules do gate | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `6.10.C3` — Confirmar que todos os ADRs Jules do Ciclo 6 têm implementação documentada | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `6.10.C4` — Executar benchmark de throughput e apresentar resultado vs SLO definido por Jules | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `6.10.C5` — Garantir que human approval é auditável e irrefutável em todos os cenários | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-migration-checklist.md`); status: **parcial/implementado**. |
| `6.2.C1` — Criar WorkflowRunner em apps/worker com BullMQ job por WorkflowRun | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `6.2.C2` — Implementar execução de steps com isolamento de contexto entre steps | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `6.2.C3` — Implementar idempotência por run_id: reprocessar run_id já completo é no-op | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/api/operacao-api-gateway.md`); status: **parcial/implementado**. |
| `6.2.C4` — Implementar pause/resume: estado serializado no banco com retomada determinística | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/migrations/20260223103000_workflow_orchestration/migration.sql`); status: **parcial/implementado**. |
| `6.2.C5` — Implementar cancel: propagar cancellation para step em execução via signal | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/migrations/20260223101000_billing_usage/migration.sql`); status: **parcial/implementado**. |
| `6.3.C1` — Criar AgentStep com: agentId version input_mapping output_mapping timeout policy_override | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `6.3.C2` — Passar tenantId actorId policyId imutáveis para cada invocação de agente no step | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_1.0.md`); status: **parcial/implementado**. |
| `6.3.C3` — Gravar output de agente como artefato do run com link no RunLog | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/migrations/20260223090000_sprint3_operational_data/migration.sql`); status: **parcial/implementado**. |
| `6.3.C4` — Criar testes de step de agente com mock do runtime retornando sucesso e falha | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `6.3.C5` — Implementar output mapping: mapear campos do output do agente para inputs do próximo step | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/migrations/20260223090000_sprint3_operational_data/migration.sql`); status: **parcial/implementado**. |

## Ciclo 07 — 20 próximos passos executados
| Passo | Execução lógica realizada | Resultado |
|---|---|---|
| `7.1.C1` — Criar schema Prisma: Plan PlanFeature Entitlement Subscription SubscriptionItem | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `7.1.C2` — Criar EntitlementService.can(tenantId, feature): boolean para todo feature gate | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `7.1.C3` — Integrar EntitlementService com QuotaService do Ciclo 2 em verificação unificada | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/product/sampling-analysis.md`); status: **parcial/implementado**. |
| `7.1.C4` — Criar seed de planos: Free Starter Pro Enterprise com features e limites configurados | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `7.1.C5` — Criar UI de comparação de planos pública com tabela de features e preços | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |
| `7.10.C1` — Executar testes de billing completos com Stripe CLI e apresentar 100% de cobertura | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./agents/shared/live_tool_client.py`); status: **parcial/implementado**. |
| `7.10.C2` — Corrigir todos os achados de billing do relatório Jules do gate | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `7.10.C3` — Confirmar que grace period e reativação funcionam com timing exato como configurado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `7.10.C4` — Garantir que nenhum endpoint de billing é acessível sem autenticação e autorização | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/fix-internal-package-exports.mjs`); status: **parcial/implementado**. |
| `7.10.C5` — Executar smoke test do fluxo completo trial → pro em staging e apresentar evidência | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `7.2.C1` — Integrar Stripe Checkout para nova assinatura com pré-preenchimento de email | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/migrations/20260223101000_billing_usage/migration.sql`); status: **parcial/implementado**. |
| `7.2.C2` — Integrar Stripe Customer Portal para gestão self-service de pagamento e plano | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/migrations/20260223101000_billing_usage/migration.sql`); status: **parcial/implementado**. |
| `7.2.C3` — Criar webhook handler para customer.subscription.created com provisionamento de entitlements | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/src/workers/webhook-worker.ts`); status: **parcial/implementado**. |
| `7.2.C4` — Criar fluxo de seleção de plano no onboarding com trial automático de 14 dias | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `7.2.C5` — Implementar checkout de upgrade/downgrade com cálculo de crédito proporcional | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `7.3.C1` — Criar POST /webhooks/stripe com verificação de Stripe-Signature header obrigatória | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `7.3.C2` — Implementar idempotência via event.id: processar evento duplicado retorna 200 sem side effect | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/compatibility.md`); status: **parcial/implementado**. |
| `7.3.C3` — Criar handler para payment_intent.succeeded com ativação de entitlements | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `7.3.C4` — Criar handler para invoice.payment_failed com início de grace period e notificação | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `7.3.C5` — Criar handler para customer.subscription.updated com sync de plano e features | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |

## Ciclo 08 — 20 próximos passos executados
| Passo | Execução lógica realizada | Resultado |
|---|---|---|
| `8.1.C1` — Configurar deploy automatizado para plataforma escolhida no ADR-026 de Jules | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/scripts/schedule-recurring-jobs.ts`); status: **parcial/implementado**. |
| `8.1.C2` — Criar Dockerfile multi-stage com distroless final para web api e worker | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `8.1.C3` — Criar pipeline de CD: branch main → staging automático → prod com aprovação manual | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/schema.prisma`); status: **parcial/implementado**. |
| `8.1.C4` — Implementar healthcheck endpoint /health com status de DB Redis e dependências | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `8.1.C5` — Criar rollback automatizado se healthcheck falhar após deploy | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `8.10.C1` — Executar restore de backup em staging isolado e apresentar evidência de sucesso | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/scripts/restore.sh`); status: **parcial/implementado**. |
| `8.10.C2` — Executar load test completo e apresentar resultado vs SLO como evidência | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `8.10.C3` — Corrigir todos os achados do relatório de SRE Sign-off de Jules | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `8.10.C4` — Confirmar que todos os runbooks Jules são executáveis por qualquer engenheiro do time | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `8.10.C5` — Executar smoke test pós-deploy em staging e apresentar evidence de healthchecks green | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `8.2.C1` — Configurar Doppler ou AWS SSM em todos os apps como fonte de secrets em runtime | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/scripts/schedule-recurring-jobs.ts`); status: **parcial/implementado**. |
| `8.2.C2` — Remover todas as variáveis sensíveis hardcoded do codebase incluindo histórico git | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |
| `8.2.C3` — Criar rotação automática de DB password e API keys internas via script agendado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `8.2.C4` — Implementar verificação de secrets na inicialização: fail fast se secret ausente ou inválido | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `8.2.C5` — Criar auditoria de acesso a secrets: quem acessou qual secret e quando | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `8.3.C1` — Configurar pg_dump diário automatizado com upload para S3 e retenção de 30 dias | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/scripts/schedule-recurring-jobs.ts`); status: **parcial/implementado**. |
| `8.3.C2` — Criar script de restore documentado e testado em ambiente de staging isolado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/package.json`); status: **parcial/implementado**. |
| `8.3.C3` — Criar alerta se backup não executar no horário esperado via job monitor | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-exemptions.md`); status: **parcial/implementado**. |
| `8.3.C4` — Implementar verificação de integridade do backup: restore em ambiente efêmero e validate | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `8.3.C5` — Criar relatório mensal de sucesso de backup com métricas de tamanho e duração | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |

## Ciclo 09 — 20 próximos passos executados
| Passo | Execução lógica realizada | Resultado |
|---|---|---|
| `9.1.C1` — Criar onboarding wizard com steps: criar org, configurar perfil, instalar pack, convidar time | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `9.1.C2` — Implementar persistência de progresso: usuário pode sair e continuar de onde parou | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `9.1.C3` — Criar progress indicator persistente no header até onboarding completo | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `9.1.C4` — Criar email de welcome com sequência: D+0 boas-vindas, D+1 primeiros passos | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/src/workers/campaign-worker.ts`); status: **parcial/implementado**. |
| `9.1.C5` — Criar analytics de onboarding: % de completude por step, tempo médio, drop-off | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `9.10.C1` — Executar onboarding wizard do zero em máquina limpa e apresentar evidência de completude | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `9.10.C2` — Corrigir todos os achados de UX do relatório Jules do gate | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./scripts/verify-tenant-isolation.sh`); status: **parcial/implementado**. |
| `9.10.C3` — Confirmar que telemetria cobre 100% dos eventos do funil de ativação | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `9.10.C4` — Garantir que todos os templates e demos funcionam sem conta real ou API key | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./CHECKLIST_MASTER.md`); status: **parcial/implementado**. |
| `9.10.C5` — Executar wizard de pack com persona não-técnica e apresentar evidência de completude | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `9.2.C1` — Criar wizard guiado de pack: marketplace → preview → conectores → teste → ativar | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `9.2.C2` — Mostrar preview de output esperado de cada agente com dados fictícios realistas | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `9.2.C3` — Criar tela de configuração de conectores com teste de conexão integrado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `9.2.C4` — Criar tela de resumo pré-instalação: o que será instalado, permissões, custo estimado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `9.2.C5` — Implementar onboarding contextual pós-instalação: próximos 3 passos sugeridos | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `9.3.C1` — Criar dashboard inicial com: agentes ativos, execuções hoje, horas economizadas (estimada) | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `9.3.C2` — Criar widgets por domínio: vendas pipelines qualificados, financeiro relatórios gerados | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `9.3.C3` — Criar empty state motivador: sem dados ainda → CTA para instalar primeiro pack | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `9.3.C4` — Implementar tour guiado na primeira visita ao dashboard com tooltips por seção | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/analysis/ssrf-via-tool-http.md`); status: **parcial/implementado**. |
| `9.3.C5` — Criar compartilhamento de snapshot do dashboard em PDF para stakeholders | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |

## Ciclo 10 — 20 próximos passos executados
| Passo | Execução lógica realizada | Resultado |
|---|---|---|
| `10.1.C1` — Criar página de marketplace com cards de packs: rating, descrição, agentes, domínio | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `10.1.C2` — Criar sistema de avaliação de pack: estrelas e review opcional por tenant | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./README.md`); status: **parcial/implementado**. |
| `10.1.C3` — Criar página de detalhes de pack com: screenshots de output, changelog, requisitos | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `10.1.C4` — Criar destaque de packs populares, novos esta semana e recomendados por domínio | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `10.1.C5` — Implementar publicação de pack customizado pelo tenant com fluxo de submissão | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `10.10.C1` — Corrigir qualquer pendência técnica identificada na revisão final de Jules | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `10.10.C2` — Executar smoke completo de todos os ciclos pós-correção e apresentar evidência final | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `10.10.C3` — Garantir que CHECKLIST_MASTER.md está 100% atualizado e reflete a realidade do repositório | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/rls-migration-checklist.md`); status: **parcial/implementado**. |
| `10.10.C4` — Confirmar que RELEASE_1.0.md está completo e revisado por Jules antes de tag | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `10.10.C5` — Executar deploy de v1.0.0 em staging e validar healthchecks de todos os serviços | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `10.2.C1` — Implementar assinatura EdDSA de manifests no pipeline de build com chave privada em vault | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/db/prisma/schema.prisma`); status: **parcial/implementado**. |
| `10.2.C2` — Implementar verificação de assinatura na instalação: pack sem assinatura válida é rejeitado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./docs/database/critical-queries-analysis.md`); status: **parcial/implementado**. |
| `10.2.C3` — Criar UI indicando se pack é 'Verificado' (assinado pela BirthHub) ou 'Comunidade' | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `10.2.C4` — Criar processo de key rotation de signing key sem invalidar packs já instalados | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/src/index.ts`); status: **parcial/implementado**. |
| `10.2.C5` — Implementar endpoint de verificação de integridade de pack instalado sob demanda | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `10.3.C1` — Criar fluxo de status de manifest: draft → submitted → in_review → approved → published | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `10.3.C2` — Criar interface de revisão de manifest para reviewer: diff, checklist inline, comentários | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./packages/queue/src/workers/base-worker.ts`); status: **parcial/implementado**. |
| `10.3.C3` — Criar notificações para autor e reviewer em cada transição de estado | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |
| `10.3.C4` — Implementar histórico de revisões com comentários e decisões arquivadas | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./RELEASE_NOTES.md`); status: **parcial/implementado**. |
| `10.3.C5` — Criar relatório de agentes pendentes de revisão com SLA de prazo | Busca de evidência com `rg` por palavras-chave do requisito. | Evidência localizada (`./SECURITY.md`); status: **parcial/implementado**. |

