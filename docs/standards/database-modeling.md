# Padrões de Modelagem de Dados

- **Tabelas:** PascalCase (no schema Prisma e banco de dados).
- **Campos:** camelCase.
- **Identificadores (IDs):** Obrigatório o uso de CUID v2.
- **Timestamps:** Todo modelo deve ter os campos `createdAt` e `updatedAt`.
