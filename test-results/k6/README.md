# Artefatos K6

Este diretório versiona os artefatos documentais do Ciclo 08 gerados por `pnpm test:load:k6`.

Arquivos esperados:

- `cycle-08-stress-summary.json`
- `cycle-08-stress-summary.txt`

Status em 2026-03-13:

- O workspace foi preparado para gerar esses arquivos automaticamente via `scripts/load-tests/stress.js`.
- O binário `k6` não estava instalado nesta sessão, então ainda não há um log de carga real anexado neste diretório.
